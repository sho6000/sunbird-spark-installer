name: Spark Platform Infra And Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (must match folder name in configs/)'
        required: true
        type: choice
        options:
          - your-env   # Replace this with your environment name (e.g. dev, prod, staging)
        default: your-env
      config_branch:
        description: 'Config repository branch (your private repo)'
        required: true
        type: string
        default: main
      source_branch:
        description: 'Source repository branch (sunbird-spark-installer - public)'
        required: true
        type: string
        default: main

      # ── JOB 1: INFRA — Core Terraform ───────────────────────────────────
      create_tf_backend:
        description: '1️⃣ Create Terraform backend'
        type: boolean
        default: false
      backup_configs:
        description: '2️⃣ Backup configurations'
        type: boolean
        default: false
      create_tf_resources:
        description: '3️⃣ Create infrastructure resources'
        type: boolean
        default: false

      # ── JOB 2: DEPLOY — Helm Bundles ────────────────────────────────────
      install_helm:
        description: '5️⃣ Install Helm components'
        type: boolean
        default: false
      helm_mode:
        description: '   ↳ "all": install all bundles | "selective": choose bundles or specific charts below'
        type: choice
        options:
          - selective
          - all
        default: selective
      monitoring:
        description: '   ↳ monitoring'
        type: boolean
        default: false
      edbb:
        description: '   ↳ edbb'
        type: boolean
        default: false
      player_image_tag:
        description: '      ↳ Player image tag (optional - leave empty for default from images.yaml)'
        type: string
        default: ''
      learnbb:
        description: '   ↳ learnbb'
        type: boolean
        default: false
      knowledgebb:
        description: '   ↳ knowledgebb'
        type: boolean
        default: false
      obsrvbb:
        description: '   ↳ obsrvbb'
        type: boolean
        default: false
      additional:
        description: '   ↳ additional'
        type: boolean
        default: false
      specific_charts:
        description: '   ↳ specific charts (space-separated, e.g. "kafka yugabyte") — also check one bundle above'
        type: string
        default: ''
      restart_workloads:
        description: '7️⃣ Restart workloads using keycloak keys'
        type: boolean
        default: false
      certificate_keys:
        description: '8️⃣ Generate certificate RSA keys (deletes existing keys first to force regeneration)'
        type: boolean
        default: false
      certificate_config:
        description: '9️⃣ Configure certificate keys'
        type: boolean
        default: false
      dns_mapping:
        description: '9️⃣ DNS mapping (waits up to 20 mins for propagation)'
        type: boolean
        default: false
      generate_postman_env:
        description: '🔟 Generate Postman environment file'
        type: boolean
        default: false
      run_post_install:
        description: '1️⃣1️⃣ Run post-install (Postman collection)'
        type: boolean
        default: false
      migrate_forms:
        description: '1️⃣3️⃣ Migrate forms (read-check then create missing)'
        type: boolean
        default: false

permissions:
  contents: read

env:
  CONFIG_PATH: "configs/${{ inputs.environment }}"
  ENV_FOLDER: "sunbird-spark-installer/opentofu/azure/${{ inputs.environment }}"
  HELMCHARTS_DIR: "sunbird-spark-installer/helmcharts"

# ════════════════════════════════════════════════════════════════════════════
# JOB 1 — INFRA (Core Terraform)
# Secrets: AZURE_INFRA_CLIENT_ID
# ════════════════════════════════════════════════════════════════════════════
jobs:
  infra:
    name: "1 — Infrastructure (Core Terraform)"
    if: ${{ inputs.create_tf_backend || inputs.backup_configs || inputs.create_tf_resources }}
    runs-on: [self-hosted, azure]
    environment: ${{ inputs.environment }}
    permissions:
      contents: write   # Encrypt and Commit Updated Configs pushes to the config repo

    steps:
      - name: Checkout Configuration Repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.config_branch }}

      - name: Clone Source Repository
        env:
          SOURCE_BRANCH: ${{ inputs.source_branch }}
        run: |
          git clone --depth 1 --branch "$SOURCE_BRANCH" \
            https://github.com/Sunbird-Spark/sunbird-spark-installer.git sunbird-spark-installer
          echo "✅ Cloned source repository (branch: $SOURCE_BRANCH)"
          echo "=== SOURCE COMMIT ==="
          git -C sunbird-spark-installer log --oneline -1

      - name: Azure Login (Managed Identity)
        run: |
          az login --identity
          echo "✅ Logged in via VM managed identity"

      - name: Decrypt Config Files
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          sudo apt-get install -y ansible -q

          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass.txt

          ansible-vault decrypt $CONFIG_PATH/global-values.yaml \
            --vault-password-file=/tmp/vault_pass.txt
          echo "✅ global-values.yaml decrypted"

          if [ -f "$CONFIG_PATH/global-cloud-values.yaml" ]; then
            ansible-vault decrypt $CONFIG_PATH/global-cloud-values.yaml \
              --vault-password-file=/tmp/vault_pass.txt
            echo "✅ global-cloud-values.yaml decrypted"
          else
            echo "⚠️  global-cloud-values.yaml not found"
          fi

          rm /tmp/vault_pass.txt

      - name: Verify Tools
        if: ${{ inputs.create_tf_backend || inputs.create_tf_resources }}
        env:
          TOFU_VERSION: "1.11.4"
        run: |
          if ! command -v tofu &>/dev/null; then
            sudo apt-get install -y unzip -q
            TOFU_VERSION="1.11.4"
            curl -sLO "https://github.com/opentofu/opentofu/releases/download/v${TOFU_VERSION}/tofu_${TOFU_VERSION}_linux_amd64.zip"
            curl -sLO "https://github.com/opentofu/opentofu/releases/download/v${TOFU_VERSION}/tofu_${TOFU_VERSION}_SHA256SUMS"
            grep " tofu_${TOFU_VERSION}_linux_amd64.zip$" "tofu_${TOFU_VERSION}_SHA256SUMS" | sha256sum -c -
            sudo unzip -qo tofu_${TOFU_VERSION}_linux_amd64.zip -d /usr/local/bin/
            rm tofu_${TOFU_VERSION}_linux_amd64.zip tofu_${TOFU_VERSION}_SHA256SUMS
          fi
          if ! command -v terragrunt &>/dev/null; then
            TERRAGRUNT_VERSION="0.77.5"
            curl -sLO "https://github.com/gruntwork-io/terragrunt/releases/download/v${TERRAGRUNT_VERSION}/terragrunt_linux_amd64"
            curl -sLO "https://github.com/gruntwork-io/terragrunt/releases/download/v${TERRAGRUNT_VERSION}/SHA256SUMS"
            grep " terragrunt_linux_amd64$" SHA256SUMS | sha256sum -c -
            sudo install -m 0755 terragrunt_linux_amd64 /usr/local/bin/terragrunt
            rm terragrunt_linux_amd64 SHA256SUMS
          fi
          if ! command -v yq &>/dev/null; then
            YQ_VERSION="4.44.1"
            curl -sLO "https://github.com/mikefarah/yq/releases/download/v${YQ_VERSION}/yq_linux_amd64"
            curl -sLO "https://github.com/mikefarah/yq/releases/download/v${YQ_VERSION}/checksums"
            curl -sLO "https://github.com/mikefarah/yq/releases/download/v${YQ_VERSION}/checksums_hashes_order"
            YQ_SHA256_FIELD=$(($(grep -n '^SHA-256$' checksums_hashes_order | cut -d: -f1) + 1))
            grep '^yq_linux_amd64 ' checksums | awk -v f="$YQ_SHA256_FIELD" '{print $f"  yq_linux_amd64"}' | sha256sum -c -
            sudo install -m 0755 yq_linux_amd64 /usr/local/bin/yq
            rm yq_linux_amd64 checksums checksums_hashes_order
          fi
          echo "/usr/local/bin" >> $GITHUB_PATH
          export PATH="/usr/local/bin:$PATH"
          tofu --version && terragrunt --version && yq --version
          echo "✅ Terraform tools ready"

      - name: Setup Environment Folder
        run: |
          cp -r sunbird-spark-installer/opentofu/azure/template $ENV_FOLDER
          cp $CONFIG_PATH/global-values.yaml $ENV_FOLDER/global-values.yaml
          if [ -f "$CONFIG_PATH/global-cloud-values.yaml" ]; then
            cp $CONFIG_PATH/global-cloud-values.yaml $ENV_FOLDER/global-cloud-values.yaml
          else
            echo "{}" > $ENV_FOLDER/global-cloud-values.yaml
          fi
          if [ -f "$CONFIG_PATH/tf.sh" ]; then
            cp $CONFIG_PATH/tf.sh $ENV_FOLDER/tf.sh
            echo "✅ tf.sh restored from configs"
          fi
          echo "✅ Environment folder ready: $ENV_FOLDER"

      - name: 1️⃣ create_tf_backend
        if: ${{ inputs.create_tf_backend }}
        run: |
          cd $ENV_FOLDER
          export TERRAGRUNT_TFPATH=/usr/local/bin/tofu
          bash install.sh create_tf_backend
          echo "✅ create_tf_backend done"

      - name: 2️⃣ backup_configs
        if: ${{ inputs.backup_configs }}
        run: |
          cd $ENV_FOLDER
          bash install.sh backup_configs
          echo "✅ backup_configs done"

      - name: 3️⃣ create_tf_resources
        if: ${{ inputs.create_tf_resources }}
        run: |
          cd $ENV_FOLDER
          export TERRAGRUNT_TFPATH=/usr/local/bin/tofu
          bash install.sh create_tf_resources
          echo "✅ create_tf_resources done"

      - name: Encrypt and Commit Updated Configs
        if: ${{ inputs.create_tf_backend || inputs.create_tf_resources }}
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass.txt

                    # Save cert keys before rsync (yq operations in keys module can corrupt/remove them)
          grep -E "^\s*CERTIFICATE(SIGN)?_(PRIVATE|PUBLIC)_KEY:" \
            $CONFIG_PATH/global-values.yaml > /tmp/cert_keys_section.txt 2>/dev/null || true

          rsync -av --exclude='.terragrunt-cache' --exclude='.terraform' \
            $ENV_FOLDER/ $CONFIG_PATH/

          # Always restore cert keys after rsync if we had them before
          if [ -s /tmp/cert_keys_section.txt ]; then
            sed -i '/^\s*CERTIFICATE_PRIVATE_KEY:/d' $CONFIG_PATH/global-values.yaml
            sed -i '/^\s*CERTIFICATE_PUBLIC_KEY:/d' $CONFIG_PATH/global-values.yaml
            sed -i '/^\s*CERTIFICATESIGN_PRIVATE_KEY:/d' $CONFIG_PATH/global-values.yaml
            sed -i '/^\s*CERTIFICATESIGN_PUBLIC_KEY:/d' $CONFIG_PATH/global-values.yaml
            cat /tmp/cert_keys_section.txt >> $CONFIG_PATH/global-values.yaml
            echo "✓ Cert keys restored after rsync"
          fi
          rm -f /tmp/cert_keys_section.txt

          for f in $CONFIG_PATH/global-values.yaml $CONFIG_PATH/global-cloud-values.yaml; do
            [ -f "$f" ] && ansible-vault encrypt "$f" --vault-password-file=/tmp/vault_pass.txt \
              && echo "✓ Encrypted: $(basename $f)"
          done

          for file in $CONFIG_PATH/*.key $CONFIG_PATH/*.pem $CONFIG_PATH/*-secrets.yaml $CONFIG_PATH/*-passwords.yaml; do
            if [ -f "$file" ] && ! grep -q "ANSIBLE_VAULT" "$file" 2>/dev/null; then
              ansible-vault encrypt "$file" --vault-password-file=/tmp/vault_pass.txt
              echo "✓ Encrypted: $(basename $file)"
            fi
          done

          rm /tmp/vault_pass.txt

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add $CONFIG_PATH/
          git commit -m "chore: encrypted configs after infra deployment [${{ inputs.environment }}] [skip ci]" \
            || echo "No changes to commit"
          git push || echo "Push failed or no changes"
          echo "✅ configs/${{ inputs.environment }}/ committed"

      - name: Infra Summary
        if: always()
        run: |
          echo "================================================"
          echo "     INFRA JOB SUMMARY"
          echo "================================================"
          echo "Environment : ${{ inputs.environment }}"
          [ "${{ inputs.create_tf_backend }}" == "true" ]   && echo "  ✅ 1️⃣  create_tf_backend"   || echo "  ⏭️  1️⃣  create_tf_backend (skipped)"
          [ "${{ inputs.backup_configs }}" == "true" ]      && echo "  ✅ 2️⃣  backup_configs"      || echo "  ⏭️  2️⃣  backup_configs (skipped)"
          [ "${{ inputs.create_tf_resources }}" == "true" ] && echo "  ✅ 3️⃣  create_tf_resources" || echo "  ⏭️  3️⃣  create_tf_resources (skipped)"
          echo "================================================"

# ════════════════════════════════════════════════════════════════════════════
# JOB 2 — DEPLOY (Helm Bundles)
# Secrets: AZURE_DEPLOY_CLIENT_ID
# ════════════════════════════════════════════════════════════════════════════
  deploy:
    name: "2 — Deploy (Helm Bundles)"
    if: ${{ always() && (inputs.install_helm || inputs.specific_charts != '') }}
    needs: [infra]
    runs-on: [self-hosted, azure]
    environment: ${{ inputs.environment }}
    permissions:
      contents: write   # Commit certificate keys after learnbb pushes to the config repo

    steps:
      - name: Checkout Configuration Repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.config_branch }}

      - name: Clone Source Repository
        env:
          SOURCE_BRANCH: ${{ inputs.source_branch }}
        run: |
          git clone --depth 1 --branch "$SOURCE_BRANCH" \
            https://github.com/Sunbird-Spark/sunbird-spark-installer.git sunbird-spark-installer
          echo "✅ Cloned source repository (branch: $SOURCE_BRANCH)"
          echo "=== SOURCE BRANCH + COMMIT ==="
          echo "input source_branch: $SOURCE_BRANCH"
          git -C sunbird-spark-installer log --oneline -1
          git -C sunbird-spark-installer rev-parse HEAD

      - name: Azure Login (Managed Identity)
        run: |
          az login --identity
          echo "✅ Logged in via VM managed identity"

      - name: Decrypt Config Files
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          sudo apt-get install -y ansible -q

          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass.txt

          ansible-vault decrypt $CONFIG_PATH/global-values.yaml \
            --vault-password-file=/tmp/vault_pass.txt
          echo "✅ global-values.yaml decrypted"

          if [ -f "$CONFIG_PATH/global-cloud-values.yaml" ]; then
            ansible-vault decrypt $CONFIG_PATH/global-cloud-values.yaml \
              --vault-password-file=/tmp/vault_pass.txt
            echo "✅ global-cloud-values.yaml decrypted"
          else
            echo "⚠️  global-cloud-values.yaml not found"
          fi

          if [ -f "$CONFIG_PATH/postman.env.json" ]; then
            cp $CONFIG_PATH/postman.env.json sunbird-spark-installer/opentofu/azure/template/
          fi

          # Addon global-cloud-values handled only in sunbird-spark-addons.yaml workflow

          rm /tmp/vault_pass.txt

      - name: Setup Environment Folder
        run: |
          cp -r sunbird-spark-installer/opentofu/azure/template $ENV_FOLDER
          cp $CONFIG_PATH/global-values.yaml $ENV_FOLDER/global-values.yaml
          if [ -f "$CONFIG_PATH/global-cloud-values.yaml" ]; then
            cp $CONFIG_PATH/global-cloud-values.yaml $ENV_FOLDER/global-cloud-values.yaml
          else
            echo "{}" > $ENV_FOLDER/global-cloud-values.yaml
          fi
          echo "✅ Environment folder ready: $ENV_FOLDER"

      - name: Verify Deploy Tools
        run: |
          kubectl version --client && helm version && yq --version
          echo "✅ Tools verified (pre-installed on runner VM)"

      - name: Get AKS Credentials
        run: |
          BUILDING_BLOCK=$(yq '.global.building_block' $CONFIG_PATH/global-values.yaml)
          CLUSTER_NAME="${BUILDING_BLOCK}-${{ inputs.environment }}"
          az aks get-credentials \
            --name "$CLUSTER_NAME" \
            --resource-group "$CLUSTER_NAME" \
            --overwrite-existing
          kubectl get nodes
          echo "✅ AKS credentials configured (cluster: $CLUSTER_NAME)"

      - name: 🔍 debug-source-state
        if: ${{ inputs.install_helm && (inputs.edbb || inputs.helm_mode == 'all') }}
        env:
          SOURCE_BRANCH: ${{ inputs.source_branch }}
          ENVIRONMENT: ${{ inputs.environment }}
        run: |
          echo "=== INPUTS ==="
          echo "source_branch input : $SOURCE_BRANCH"
          echo "environment         : $ENVIRONMENT"
          echo ""
          echo "=== CLONED REPO ==="
          cd sunbird-spark-installer
          echo "HEAD commit : $(git log --oneline -1)"
          echo "SHA         : $(git rev-parse HEAD)"
          echo ""
          echo "=== knowledgemw/templates/configmap.yaml lines 13-28 ==="
          sed -n '13,28p' helmcharts/edbb/charts/knowledgemw/templates/configmap.yaml | cat -n
          echo ""
          echo "=== helm version ==="
          helm version --short
          echo ""
          echo "=== RENDER knowledgemw/configmap ==="
          cd $GITHUB_WORKSPACE
          helm template edbb $HELMCHARTS_DIR/edbb --namespace sunbird \
            -f $HELMCHARTS_DIR/edbb/values.yaml \
            -f $HELMCHARTS_DIR/images.yaml \
            -f $HELMCHARTS_DIR/global-resources.yaml \
            -f $ENV_FOLDER/global-values.yaml \
            -f $ENV_FOLDER/global-cloud-values.yaml \
            --show-only charts/knowledgemw/templates/configmap.yaml 2>&1 | cat -n | sed -n '1,30p'

      - name: 5️⃣ install_helm — all bundles
        if: ${{ inputs.install_helm && inputs.helm_mode == 'all' }}
        run: |
          cd $ENV_FOLDER
          bash install.sh install_helm_components
          echo "✅ All Helm components installed"

      - name: 5️⃣ install_helm — monitoring
        if: ${{ inputs.install_helm && inputs.helm_mode == 'selective' && inputs.monitoring && inputs.specific_charts == '' }}
        run: |
          cd $ENV_FOLDER && bash install.sh install_component monitoring
          echo "✅ monitoring installed"

      - name: 5️⃣ install_helm — edbb
        if: ${{ inputs.install_helm && inputs.helm_mode == 'selective' && inputs.edbb && inputs.specific_charts == '' }}
        env:
          PLAYER_IMAGE_TAG: ${{ inputs.player_image_tag }}
        run: |
          if [ -n "$PLAYER_IMAGE_TAG" ]; then
            yq e ".player.tag = strenv(PLAYER_IMAGE_TAG)" -i $HELMCHARTS_DIR/images.yaml
          fi
          cd $ENV_FOLDER && bash install.sh install_component edbb
          echo "✅ edbb installed"

      - name: 5️⃣ install_helm — learnbb
        if: ${{ inputs.install_helm && inputs.helm_mode == 'selective' && inputs.learnbb && inputs.specific_charts == '' }}
        run: |
          cd $ENV_FOLDER && bash install.sh install_component learnbb
          echo "✅ learnbb installed"

      - name: 5️⃣ Commit certificate keys after learnbb
        if: ${{ inputs.install_helm && (inputs.learnbb || inputs.helm_mode == 'all') && inputs.specific_charts == '' }}
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass.txt

          # Copy back global-values.yaml (may have cert keys appended)
          cp $ENV_FOLDER/global-values.yaml $CONFIG_PATH/global-values.yaml

          # If cert keys missing from global-values.yaml, regenerate them
          if ! grep -q "CERTIFICATE_PRIVATE_KEY" $ENV_FOLDER/global-values.yaml 2>/dev/null; then
            echo "⚠️  CERTIFICATE keys missing — running certificate_keys()..."
            cd $ENV_FOLDER && bash install.sh certificate_keys
            cp $ENV_FOLDER/global-values.yaml $CONFIG_PATH/global-values.yaml
            echo "✓ Certificate keys generated and copied"
          fi

          # Copy pem files if generated by certificate_keys()
          for f in certkey.pem certpubkey.pem; do
            if [ -f "$ENV_FOLDER/$f" ]; then
              cp $ENV_FOLDER/$f $CONFIG_PATH/$f
              echo "✓ Copied $f"
            fi
          done

          # Encrypt only cert-related files
          for f in $CONFIG_PATH/global-values.yaml $CONFIG_PATH/certkey.pem $CONFIG_PATH/certpubkey.pem; do
            [ -f "$f" ] && ! grep -q "ANSIBLE_VAULT" "$f" && \
              ansible-vault encrypt "$f" --vault-password-file=/tmp/vault_pass.txt && \
              echo "✓ Encrypted: $(basename $f)"
          done

          rm /tmp/vault_pass.txt

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Add ONLY cert files — do NOT use git add $CONFIG_PATH/ (avoids committing decrypted cloud values)
          git add $CONFIG_PATH/global-values.yaml
          [ -f "$CONFIG_PATH/certkey.pem" ]    && git add $CONFIG_PATH/certkey.pem
          [ -f "$CONFIG_PATH/certpubkey.pem" ] && git add $CONFIG_PATH/certpubkey.pem
          git commit -m "chore: save certificate keys after learnbb deploy [${{ inputs.environment }}] [skip ci]" \
            || echo "No changes to commit"
          git push || echo "Push failed or no changes"
          echo "✅ Certificate keys committed"

      - name: 5️⃣ install_helm — knowledgebb
        if: ${{ inputs.install_helm && inputs.helm_mode == 'selective' && inputs.knowledgebb && inputs.specific_charts == '' }}
        run: |
          cd $ENV_FOLDER && bash install.sh install_component knowledgebb
          echo "✅ knowledgebb installed"

      - name: 5️⃣ install_helm — obsrvbb
        if: ${{ inputs.install_helm && inputs.helm_mode == 'selective' && inputs.obsrvbb && inputs.specific_charts == '' }}
        run: |
          cd $ENV_FOLDER && bash install.sh install_component obsrvbb
          echo "✅ obsrvbb installed"

      - name: 5️⃣ install_helm — additional
        if: ${{ inputs.install_helm && inputs.helm_mode == 'selective' && inputs.additional && inputs.specific_charts == '' }}
        run: |
          cd $ENV_FOLDER && bash install.sh install_component additional
          echo "✅ additional installed"

      - name: 5️⃣ install_helm — specific charts
        if: ${{ inputs.specific_charts != '' }}
        env:
          SPECIFIC_CHARTS: ${{ inputs.specific_charts }}
        run: |
          cd $ENV_FOLDER
          bundle=""
          [ "${{ inputs.monitoring }}"  == "true" ] && bundle="monitoring"
          [ "${{ inputs.edbb }}"        == "true" ] && bundle="edbb"
          [ "${{ inputs.learnbb }}"     == "true" ] && bundle="learnbb"
          [ "${{ inputs.knowledgebb }}" == "true" ] && bundle="knowledgebb"
          [ "${{ inputs.obsrvbb }}"     == "true" ] && bundle="obsrvbb"
          [ "${{ inputs.additional }}"  == "true" ] && bundle="additional"
          if [ -z "$bundle" ]; then
            echo "❌ No bundle selected — check one bundle checkbox alongside specific_charts"
            exit 1
          fi
          bash install.sh install_service "$bundle" $SPECIFIC_CHARTS
          echo "✅ specific charts ($SPECIFIC_CHARTS) installed in bundle: $bundle"

      - name: Deploy Summary
        if: always()
        env:
          SPECIFIC_CHARTS: ${{ inputs.specific_charts }}
        run: |
          echo "================================================"
          echo "     DEPLOY JOB SUMMARY"
          echo "================================================"
          echo "Environment : ${{ inputs.environment }}"
          if [ "${{ inputs.install_helm }}" == "true" ]; then
            echo "  ✅ 5️⃣  install_helm (mode: ${{ inputs.helm_mode }})"
            [ "${{ inputs.helm_mode }}" == "selective" ] && {
              [ "${{ inputs.monitoring }}"  == "true" ] && echo "       ✅ monitoring"  || echo "       ⏭️  monitoring"
              [ "${{ inputs.edbb }}"        == "true" ] && echo "       ✅ edbb"        || echo "       ⏭️  edbb"
              [ "${{ inputs.learnbb }}"     == "true" ] && echo "       ✅ learnbb"     || echo "       ⏭️  learnbb"
              [ "${{ inputs.knowledgebb }}" == "true" ] && echo "       ✅ knowledgebb" || echo "       ⏭️  knowledgebb"
              [ "${{ inputs.obsrvbb }}"     == "true" ] && echo "       ✅ obsrvbb"     || echo "       ⏭️  obsrvbb"
              [ "${{ inputs.additional }}"  == "true" ] && echo "       ✅ additional"  || echo "       ⏭️  additional"
            }
          else
            echo "  ⏭️  5️⃣  install_helm (skipped)"
          fi
          if [ -n "$SPECIFIC_CHARTS" ]; then
            bundle=""
            [ "${{ inputs.monitoring }}"  == "true" ] && bundle="monitoring"
            [ "${{ inputs.edbb }}"        == "true" ] && bundle="edbb"
            [ "${{ inputs.learnbb }}"     == "true" ] && bundle="learnbb"
            [ "${{ inputs.knowledgebb }}" == "true" ] && bundle="knowledgebb"
            [ "${{ inputs.obsrvbb }}"     == "true" ] && bundle="obsrvbb"
            [ "${{ inputs.additional }}"  == "true" ] && bundle="additional"
            echo "  ✅ 5️⃣  specific charts in ${bundle:-<no bundle>}: $SPECIFIC_CHARTS"
          fi
          echo "================================================"

# ════════════════════════════════════════════════════════════════════════════
# JOB 3 — PLATFORM (Post-Install Config)
# Secrets: AZURE_DEPLOY_CLIENT_ID
# ════════════════════════════════════════════════════════════════════════════
  platform:
    name: "3 — Platform (Post-Install Config)"
    if: ${{ always() && (inputs.restart_workloads || inputs.certificate_config || inputs.dns_mapping || inputs.generate_postman_env || inputs.run_post_install || inputs.migrate_forms) }}
    needs: [deploy]
    runs-on: [self-hosted, azure]
    environment: ${{ inputs.environment }}
    permissions:
      contents: write   # Encrypt and Commit env.json pushes to the config repo

    steps:
      - name: Checkout Configuration Repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.config_branch }}

      - name: Clone Source Repository
        env:
          SOURCE_BRANCH: ${{ inputs.source_branch }}
        run: |
          git clone --depth 1 --branch "$SOURCE_BRANCH" \
            https://github.com/Sunbird-Spark/sunbird-spark-installer.git sunbird-spark-installer
          echo "✅ Cloned source repository (branch: $SOURCE_BRANCH)"

      - name: Azure Login (Managed Identity)
        run: |
          az login --identity
          echo "✅ Logged in via VM managed identity"

      - name: Decrypt Config Files
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          sudo apt-get install -y ansible -q

          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass.txt

          ansible-vault decrypt $CONFIG_PATH/global-values.yaml \
            --vault-password-file=/tmp/vault_pass.txt
          echo "✅ global-values.yaml decrypted"

          if [ -f "$CONFIG_PATH/global-cloud-values.yaml" ]; then
            ansible-vault decrypt $CONFIG_PATH/global-cloud-values.yaml \
              --vault-password-file=/tmp/vault_pass.txt
            echo "✅ global-cloud-values.yaml decrypted"
          else
            echo "⚠️  global-cloud-values.yaml not found"
          fi

          if [ -f "$CONFIG_PATH/postman.env.json" ]; then
            cp $CONFIG_PATH/postman.env.json sunbird-spark-installer/opentofu/azure/template/
          fi

          if [ -f "$CONFIG_PATH/env.json" ]; then
            ansible-vault decrypt $CONFIG_PATH/env.json \
              --vault-password-file=/tmp/vault_pass.txt
            cp $CONFIG_PATH/env.json sunbird-spark-installer/opentofu/azure/template/
            echo "✅ env.json decrypted and staged"
          fi

          rm /tmp/vault_pass.txt

      - name: Setup Environment Folder
        run: |
          cp -r sunbird-spark-installer/opentofu/azure/template $ENV_FOLDER
          cp $CONFIG_PATH/global-values.yaml $ENV_FOLDER/global-values.yaml
          if [ -f "$CONFIG_PATH/global-cloud-values.yaml" ]; then
            cp $CONFIG_PATH/global-cloud-values.yaml $ENV_FOLDER/global-cloud-values.yaml
          else
            echo "{}" > $ENV_FOLDER/global-cloud-values.yaml
          fi
          echo "✅ Environment folder ready: $ENV_FOLDER"

      - name: Install Platform Tools
        env:
          # Postman doesn't publish an official checksum for this script (no
          # .sha256 sibling file at the same URL). Pinned to the checksum of
          # the version reviewed while fixing this -- if the script's
          # legitimately changed upstream, re-review it and update this
          # value; don't just drop the check.
          POSTMAN_INSTALL_SHA256: "7d1f5f3d138ab09a656794a33c9ff1c2b7315c4cce2200b62ef49273e6cc823b"
        run: |
          if ! command -v postman &>/dev/null; then
            curl -fsSL --proto '=https' --tlsv1.2 -o postman-install.sh "https://dl-cli.pstmn.io/install/linux64.sh"
            echo "${POSTMAN_INSTALL_SHA256}  postman-install.sh" | sha256sum -c -
            sh postman-install.sh
            rm -f postman-install.sh
          fi
          kubectl version --client && helm version && yq --version && openssl version && postman --version
          echo "✅ Platform tools ready"

      - name: Get AKS Credentials
        run: |
          BUILDING_BLOCK=$(yq '.global.building_block' $CONFIG_PATH/global-values.yaml)
          CLUSTER_NAME="${BUILDING_BLOCK}-${{ inputs.environment }}"
          az aks get-credentials \
            --name "$CLUSTER_NAME" \
            --resource-group "$CLUSTER_NAME" \
            --overwrite-existing
          kubectl get nodes
          echo "✅ AKS credentials configured (cluster: $CLUSTER_NAME)"

      - name: 7️⃣ restart_workloads
        if: ${{ inputs.restart_workloads }}
        run: |
          cd $ENV_FOLDER && bash install.sh restart_workloads_using_keys
          echo "✅ restart_workloads done"

      - name: 8️⃣ certificate_config
        if: ${{ inputs.certificate_config }}
        run: |
          cd $ENV_FOLDER && bash install.sh certificate_config
          echo "✅ certificate_config done"

      - name: 9️⃣ dns_mapping
        if: ${{ inputs.dns_mapping }}
        run: |
          cd $ENV_FOLDER && bash install.sh dns_mapping
          echo "✅ dns_mapping done"

      - name: 🔟 generate_postman_env
        if: ${{ inputs.generate_postman_env }}
        run: |
          cd $ENV_FOLDER && bash install.sh generate_postman_env
          echo "✅ generate_postman_env done"

      - name: Encrypt and Commit env.json
        if: ${{ inputs.generate_postman_env }}
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          cp $ENV_FOLDER/env.json $CONFIG_PATH/env.json

          ansible-vault encrypt $CONFIG_PATH/env.json \
            --vault-password-file=<(echo "$ANSIBLE_VAULT_PASSWORD")

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add $CONFIG_PATH/env.json
          git commit -m "Add encrypted env.json [${{ inputs.environment }}] [skip ci]" || echo "No changes to commit"
          git push || echo "Push failed or no changes"
          echo "✅ env.json committed"

      - name: 1️⃣1️⃣ run_post_install
        if: ${{ inputs.run_post_install }}
        run: |
          cd $ENV_FOLDER && bash install.sh run_post_install
          echo "✅ run_post_install done"

      - name: 1️⃣3️⃣ migrate_forms
        if: ${{ inputs.migrate_forms }}
        run: |
          cd $ENV_FOLDER && bash install.sh migrate_forms
          echo "✅ migrate_forms done"

      - name: Platform Summary
        if: always()
        run: |
          echo "================================================"
          echo "     PLATFORM JOB SUMMARY"
          echo "================================================"
          echo "Environment : ${{ inputs.environment }}"
          [ "${{ inputs.restart_workloads }}"     == "true" ] && echo "  ✅ 7️⃣   restart_workloads"    || echo "  ⏭️  7️⃣   restart_workloads (skipped)"
          [ "${{ inputs.certificate_config }}"   == "true" ] && echo "  ✅ 8️⃣   certificate_config"   || echo "  ⏭️  8️⃣   certificate_config (skipped)"
          [ "${{ inputs.dns_mapping }}"          == "true" ] && echo "  ✅ 9️⃣   dns_mapping"          || echo "  ⏭️  9️⃣   dns_mapping (skipped)"
          [ "${{ inputs.generate_postman_env }}" == "true" ] && echo "  ✅ 🔟   generate_postman_env" || echo "  ⏭️  🔟   generate_postman_env (skipped)"
          [ "${{ inputs.run_post_install }}"     == "true" ] && echo "  ✅ 1️⃣1️⃣  run_post_install"     || echo "  ⏭️  1️⃣1️⃣  run_post_install (skipped)"
          [ "${{ inputs.migrate_forms }}"          == "true" ] && echo "  ✅ 1️⃣3️⃣  migrate_forms"          || echo "  ⏭️  1️⃣3️⃣  migrate_forms (skipped)"
          echo "================================================"
