fullname: kbb

knowlg_automation_repo_branch: main

channel: &channel
  default: "in.ekstep"

knowlg:
  enabled: true
  opa_enabled: true

job_cleaner_kubectl_image:
  registry: "sunbirded.azurecr.io"
  repository: "kubectl"
  tag: "1.28.4"

provisioningAnnotations: &provisioningAnnotations
  "helm.sh/hook-weight": "-4"

migration:
  yugabyte:
    waitTime: 120

opensearch: &opensearch
  enabled: true
  fullnameOverride: "opensearch"
  host: "opensearch-cluster-master"
  port: 9200
  singleNode: true
  extraEnvs:
    - name: OPENSEARCH_INITIAL_ADMIN_PASSWORD
      value: ""
  config:
    opensearch.yml: |
      plugins.security.disabled: true
      search.insights.top_queries.latency.enabled: false
      search.insights.top_queries.cpu.enabled: false
      search.insights.top_queries.memory.enabled: false
  commonAnnotations:
    "helm.sh/hook-weight": "-5"

graph_passport: &graph_passport
  key_base: "secretKey"

installation: &installation
  id: ekstep

janusgraph: &janusgraph
  enabled: true
  fullnameOverride: "janusgraph"
  nameOverride: "janusgraph"
  host: "janusgraph"
  port: 8182
  image:
    registry: docker.io
    repository: bitnamilegacy/janusgraph
    tag: 1.1.0-debian-12-r21
  commonAnnotations:
    "helm.sh/hook-weight": "-5"
  provisioning:
    annotations: *provisioningAnnotations 

kafka: &kafka
  enabled: true
  nameOverride: "kafka"
  fullnameOverride: "kafka"
  clientProtocol: PLAINTEXT
  host: "kafka"
  port: 9092
  image:
    registry: sunbirded.azurecr.io
    repository: kafka
    tag: 3.3.1-debian-11-r25
  commonAnnotations:
    "helm.sh/hook-weight": "-5"
  persistence:
    size: 25Gi
  provisioning:
    annotations: *provisioningAnnotations
    replicationFactor: 1
    numPartitions: 1
    topics:
    - name: "{{ .Values.global.env }}.knowlg.publish.job.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.content.postpublish.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.learning.job.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.learning.graph.events"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.learning.events.failed"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.telemetry.raw"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.auto.creation.job.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.transaction.meta"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.qrimage.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.system.events"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.dialcode.context.job.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.dialcode.context.job.request.failed"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.enriched.content.metadata"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.knowlg.embedding.chunks"
      partitions: 1
      replicationFactor: 1
    # ai-pipeline (PyFlink) jobs: enrichment-router / caption-generator.
    # enrichment-router's input is the existing
    # {{ .Values.global.env }}.knowlg.enriched.content.metadata topic above
    # (real content-publish events) — not a separate topic.
    - name: "{{ .Values.global.env }}.media.transcription.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.media.multilingual.request"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.media.transcription.dlq"
      partitions: 1
      replicationFactor: 1
    - name: "{{ .Values.global.env }}.media.multilingual.dlq"
      partitions: 1
      replicationFactor: 1
    parallel: 1
  listeners:
    client:
      protocol: PLAINTEXT
  controller:
      protocol: PLAINTEXT
  interbroker:
      protocol: PLAINTEXT
  external:
      protocol: PLAINTEXT
  sasl:
    enabledMechanisms: ""
    interBrokerMechanism: ""
    controllerMechanism: ""
  tls:
    autoGenerated:
      enabled: false
    existingSecret: ""

objectcategorydefinition: &objectcategorydefinition
  keyspace: "{{ .Values.global.env }}_category_store"

# play_http: &play_http
#   secret_key: "secretKey"

redis: &redis
  enabled: true
  nameOverride: "redis"
  fullnameOverride: "redis"
  auth:
    enabled: false
  host: "redis-master"
  port: 6379
  commonAnnotations:
    "helm.sh/hook-weight": "-5"
  replica:
    replicaCount: 0
  master:
    persistence:
      size: 25Gi

schema: &schema
  base_path: "https://{{ .Values.global.object_storage_endpoint }}/{{ .Values.global.public_container_name }}/schemas/local"

yugabyte: &yugabyte
  enabled: true
  nameOverride: "yugabyte"
  fullnameOverride: "yugabyte"
  host: "yb-tserver-service" 
  port: 9042
  postgresql_port: 5433
  image:
    registry: yugabytedb
    repository: yugabyte
    tag: 2025.2.0.0-b131 
  commonAnnotations:
    "helm.sh/hook-weight": "-5"
  provisioning:
    annotations: *provisioningAnnotations
  # Force single replica for Yugabyte masters and tservers (override subchart defaults)
  replicas:
    master: 3
    tserver: 3   
  username: yugabyte
  password: "yugabyte"    

yugabytecql:
  image:
    repository: "ghcr.io/sunbird-spark/sunbird-yugabyte-migrations"
    tag: "develop_499032f"

dialservice: &dialservice
  base_url: "http://dial-service:9000"

sync-tool:
  enabled: false

search:
  enabled: true

flink:
  enabled: true

py-flink:
  enabled: true

global:
  channel: *channel
  opensearch: *opensearch
  graph_passport: *graph_passport
  installation: *installation
  janusgraph: *janusgraph
  kafka: *kafka
  keycloak_kid_keys_configmap: keycloak-kids-keys
  kong_desktop_device_consumer_names_for_opa: '["portal", "desktop"]'
  objectcategorydefinition: *objectcategorydefinition
  # play_http: *play_http
  provisioningAnnotations: *provisioningAnnotations
  redis: *redis
  schema: *schema
  yugabyte: *yugabyte
  dialservice: *dialservice