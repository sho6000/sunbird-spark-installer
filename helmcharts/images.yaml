image:
  dockerRegistrySecretName: "registry-secret-name"
  dockerConfigJson: |-
    {"auths":{"http://registry.your-domain.io":{"auth":"eW91cl9yZWdpc3RyeV91c2VybmFtZTp5b3VyX3JlZ2lzdHJ5X3Rva2Vu"}}}

imagePullSecrets:
  - name: "registry-secret-name"

# All images defined with anchors
images:
  velero: &velero
    registry: "docker.io"
    repository: "velero/velero"
    tag: "v1.15.0"

  volume_autoscaler: &volume_autoscaler
    registry: "docker.io"
    repository: "devopsnirvana/kubernetes-volume-autoscaler"
    tag: "1.0.6"

  kafka: &kafka
    registry: "docker.io"
    repository: sanketikahub/kafka
    tag: "4.0.0"

  redis: &redis
    registry: "sunbirded.azurecr.io"
    repository: "redis"
    tag: "7.0.9-debian-11-r1"

  opensearch: &opensearch
    registry: "docker.io"
    repository: "opensearchproject/opensearch"
    tag: "2.19.5"

  alloy: &alloy
    registry: "docker.io"
    repository: grafana/alloy
    tag: v1.12.1

  echo: &echo
    repository: "ghcr.io/project-sunbird/sunbird-echo-service"
    tag: "release-7.7.0_rc1_50b5b2d_2"

  player: &player
    repository: "ghcr.io/sunbird-spark/sunbird-spark-portal"
    tag: "spark-v1.1.0_ce97ee1"

  router: &router
    repository: "sunbirded.azurecr.io/router"
    tag: "release-3.9.0_RC1_01339f9_07941f0_1_router"

  kong: &kong
    registry: "docker.io"
    repository: "kong"
    tag: "3.9.1"

  kong_apis: &kong_apis
    repository: "ghcr.io/sunbird-spark/kong-api-scripts"
    tag: "spark-v1.1.1-test-openssf-scorecard_bb74aa0"

  kong_consumers: &kong_consumers
    repository: "ghcr.io/sunbird-spark/kong-api-scripts"
    tag: "spark-v1.1.1-test-openssf-scorecard_bb74aa0"

  knowledge_mw: &knowledge_mw
    repository: "ghcr.io/sunbird-knowlg/knowledge-mw-service"
    tag: "spark-v1.1.0_dbad626_19"
    
  nginx_private_ingress: &nginx_private_ingress
    repository: "sunbirded.azurecr.io/proxy"
    tag: "release-7.0.0_8e6b2db_23"

  nginx_public_ingress: &nginx_public_ingress
    repository: "sunbirded.azurecr.io/proxy"
    tag: "release-7.0.0_8e6b2db_23"

  adminutil: &adminutil
    repository: "ghcr.io/sunbird-lern/sunbird-apimanager-util"
    tag: "spark-v1.1.0_a7d8c3f_6"

  cert: &cert
    repository: "ghcr.io/sunbird-lern/cert-service"
    tag: "release-8.0.0_rc1_ba5cc75_1"

  certregistry: &certregistry
    repository: "ghcr.io/sunbird-lern/certificate-registry"
    tag: "release-8.1.0_rc2_546e8d5_3"

  certificateapi: &certificateapi
    repository: "sunbirded.azurecr.io/sunbird-rc-certificate-api"
    tag: "release-5.1.0_2bc7760_10"

  certificatesign: &certificatesign
    repository: "sunbirded.azurecr.io/sunbird-rc-certificate-signer"
    tag: "release-5.1.0_2bc7760_9"

  keycloak: &keycloak
    repository: "ghcr.io/sunbird-spark/keycloak-21.1.2"
    tag: "spark-v1.1.1-test-openssf-scorecard_bb74aa0"

  keycloak-kids-keys: &keycloak-kids-keys
    repository: "ghcr.io/sunbird-spark/keycloak-postgres-query"
    tag: "spark-v1.1.1-test-openssf-scorecard_bb74aa0"

  lern: &lern
    repository: "ghcr.io/sunbird-lern/lern-service"
    tag: "spark-v1.1.0_1426176_76"
    
  registry: &registry
    repository: "sunbirded.azurecr.io/sunbird-rc-core"
    tag: "sparkv0.0.13.1"

  knowlg: &knowlg
    repository: "ghcr.io/sunbird-knowlg/knowledge-platform/knowlg-service"
    tag: "spark-v1.1.0_4a35a67"

  search: &search
    repository: "ghcr.io/sunbird-knowlg/knowledge-platform/search-api"
    tag: "spark-v1.1.0_4a35a67"

  telemetry: &telemetry
    repository: "ghcr.io/project-sunbird/sunbird-telemetry-service"
    tag: "v8.1.3-rc1_c4cc359_18"

  # superset's chart template only reads .image.repository + .image.tag,
  # it never reads .image.registry — repository has to be the full combined
  # path or every superset pod silently tries to pull from docker.io instead
  # of sanketikahub (confirmed via templates/deployment*.yaml)
  superset: &superset
    repository: "sanketikahub/superset"
    tag: "4.1.1"

  nlweb: &nlweb
    repository: "sanketikahub/nlweb"
    tag: "0.0.1"

  nlwebflink: &nlwebflink
    repository: "sunbirded.azurecr.io/flink-nlweb-job"
    tag: "0.0.3"

  yugabyte: &yugabyte
    repository: "yugabytedb/yugabyte"
    tag: "2025.2.0.0-b131"

  janusgraph: &janusgraph
    registry: ghcr.io
    repository: sunbird-spark/janusgraph
    tag: spark-v1.1.1-test-openssf-scorecard_bb74aa0

  secor: &secor
    repository: "sunbirded.azurecr.io/secor"
    tag: "spark"

  sync_tool: &sync_tool
    repository: "ghcr.io/sunbird-knowlg/knowledge-platform/sync-tool"
    tag: "spark-v1.0.2_448714d"

  yugabytecql: &yugabytecql
    repository: "ghcr.io/sunbird-spark/sunbird-yugabyte-migrations"
    tag: "spark-v1.1.1-test-openssf-scorecard_bb74aa0"

  yugabyte_backup: &yugabyte_backup
    repository: "ghcr.io/sunbird-spark/yugabyte-backup"
    tag: "spark-v1.1.1-test-openssf-scorecard_bb74aa0"

  # kube-prometheus-stack's own bundled images — previously entirely
  # untracked here, running whatever that chart's own defaults were.
  prometheus_operator: &prometheus_operator
    registry: "quay.io"
    repository: "prometheus-operator/prometheus-operator"
    tag: "v0.68.0"

  # Not part of this migration's DHI scope (no equivalent identified yet) -
  # centralized as-is so this override is at least trackable/visible instead
  # of silently relying on the vendored subchart's own default.
  kube_webhook_certgen: &kube_webhook_certgen
    registry: "registry.k8s.io"
    repository: "ingress-nginx/kube-webhook-certgen"
    tag: "v20221220-controller-v1.5.1-58-g787ea74b6"

  prometheus: &prometheus
    registry: "quay.io"
    repository: "prometheus/prometheus"
    tag: "v2.47.1"

  alertmanager: &alertmanager
    registry: "quay.io"
    repository: "prometheus/alertmanager"
    tag: "v0.26.0"

  blackbox_exporter: &blackbox_exporter
    registry: "quay.io"
    repository: "prometheus/blackbox-exporter"
    tag: "v0.24.0"

  kube_state_metrics: &kube_state_metrics
    registry: "registry.k8s.io"
    repository: "kube-state-metrics/kube-state-metrics"
    tag: "v2.10.0"

  node_exporter: &node_exporter
    registry: "quay.io"
    repository: "prometheus/node-exporter"
    tag: "v1.6.1"

  # this subchart's own image field is repository (combined) + imageTag,
  # not registry/repository/tag like the others above
  statsd_exporter: &statsd_exporter
    repository: "quay.io/prometheus/statsd-exporter"
    imageTag: "v0.17.0"

  k8s_sidecar: &k8s_sidecar
    repository: "quay.io/kiwigrid/k8s-sidecar"
    tag: "1.24.3"

  grafana: &grafana
    registry: "docker.io"
    repository: "grafana/grafana"
    tag: "11.4.0"

  # kubernetes-dashboard — separate top-level dependency under monitoring,
  # not part of kube-prometheus-stack
  dashboard_api: &dashboard_api
    repository: "docker.io/kubernetesui/dashboard-api"
    tag: "v1.0.0"

  dashboard_web: &dashboard_web
    repository: "docker.io/kubernetesui/dashboard-web"
    tag: "v1.0.0"

  dashboard_metrics_scraper: &dashboard_metrics_scraper
    repository: "docker.io/kubernetesui/metrics-scraper"
    tag: "v1.0.9"

  # this initContainer's image field is a plain "repo:tag" string, not a map
  velero_plugin_azure: &velero_plugin_azure "velero/velero-plugin-for-microsoft-azure:v1.2.0"

  velero_kubectl: &velero_kubectl
    repository: "sunbirded.azurecr.io/kubectl"
    tag: "1.26.14-debian-11-r6"

  loki: &loki
    registry: "docker.io"
    repository: "grafana/loki"
    tag: "2.9.3"

  loki_gateway: &loki_gateway
    registry: "docker.io"
    repository: "nginxinc/nginx-unprivileged"
    tag: "1.24-alpine"

  # this subchart's own image field is name (combined) + tag, not repository
  reloader: &reloader
    name: "ghcr.io/stakater/reloader"
    tag: "v1.0.60"

  # this sidecar's image field is also a plain "repo:tag" string
  janusgraph_logstash: &janusgraph_logstash "docker.elastic.co/logstash/logstash-oss:7.10.2"

  # these seven were hardcoded literals directly in template source (no
  # values.yaml field existed at all) until they were templatized alongside
  # this same commit — see templates/provision/job-cleaner.yaml (x3 bbs),
  # templates/provision/ysql.yaml, templates/provision/opensearch-migration.yaml,
  # and knowledgemw/templates/deployment.yaml
  job_cleaner_kubectl_image: &job_cleaner_kubectl_image
    registry: "sunbirded.azurecr.io"
    repository: "kubectl"
    tag: "1.28.4"

  job_wait_image: &job_wait_image
    registry: "docker.io"
    repository: "busybox"
    tag: "latest"

  yugabyte_migration_image: &yugabyte_migration_image
    registry: "docker.io"
    repository: "yugabytedb/yugabyte"
    tag: "latest"

  opensearch_migration_image: &opensearch_migration_image
    registry: "docker.io"
    repository: "curlimages/curl"
    tag: "latest"

  envoy_image: &envoy_image
    registry: "docker.io"
    repository: "envoyproxy/envoy"
    tag: "v1.20.0"

  opa_image: &opa_image
    registry: "sunbirded.azurecr.io"
    repository: "opa"
    tag: "0.34.2-envoy"

  proxy_init_image: &proxy_init_image
    registry: "docker.io"
    repository: "openpolicyagent/proxy_init"
    tag: "v5"

# Internal block with pull secrets
internal: &internal
  velero:
    image: *velero
    imagePullSecrets:
      - name: "registry-secret-name"

  volume_autoscaler:
    image: *volume_autoscaler
    imagePullSecrets:
      - name: "registry-secret-name"

  kafka:
    image: *kafka
    imagePullSecrets:
      - name: "registry-secret-name"

  redis:
    image: *redis
    imagePullSecrets:
      - name: "registry-secret-name"

  opensearch:
    image: *opensearch
    imagePullSecrets:
      - name: "registry-secret-name"

  alloy:
    image: *alloy
    imagePullSecrets:
      - name: "registry-secret-name"   

  knowlg:
    image: *knowlg
    imagePullSecrets:
      - name: "registry-secret-name"      

  echo:
    image: *echo
    imagePullSecrets:
      - name: "registry-secret-name"

  player:
    image: *player
    imagePullSecrets:
      - name: "registry-secret-name"

  router:
    image: *router
    imagePullSecrets:
      - name: "registry-secret-name"


  kong:
    image: *kong
    imagePullSecrets:
      - name: "registry-secret-name"

  kong-apis:
    image: *kong_apis
    imagePullSecrets:
      - name: "registry-secret-name"

  kong-consumers:
    image: *kong_consumers
    imagePullSecrets:
      - name: "registry-secret-name"

  knowledgemw:
    image: *knowledge_mw
    imagePullSecrets:
      - name: "registry-secret-name"

  nginx-private-ingress:
    image: *nginx_private_ingress
    imagePullSecrets:
      - name: "registry-secret-name"

  nginx-public-ingress:
    image: *nginx_public_ingress
    imagePullSecrets:
      - name: "registry-secret-name"    

  adminutil:
    image: *adminutil
    imagePullSecrets:
      - name: "registry-secret-name"

  cert:
    image: *cert
    imagePullSecrets:
      - name: "registry-secret-name"

  certregistry:
    image: *certregistry
    imagePullSecrets:
      - name: "registry-secret-name"

  certificateapi:
    image: *certificateapi
    imagePullSecrets:
      - name: "registry-secret-name"

  certificatesign:
    image: *certificatesign
    imagePullSecrets:
      - name: "registry-secret-name"

  keycloak:
    image: *keycloak
    imagePullSecrets:
      - name: "registry-secret-name"

  keycloak-kids-keys:
    image: *keycloak-kids-keys
    imagePullSecrets:
      - name: "registry-secret-name"

  lern:
    image: *lern
    imagePullSecrets:
      - name: "registry-secret-name"
  
  registry:
    image: *registry
    imagePullSecrets:
      - name: "registry-secret-name"

  search:
    image: *search
    imagePullSecrets:
      - name: "registry-secret-name"

  telemetry:
    image: *telemetry
    imagePullSecrets:
      - name: "registry-secret-name"

  superset:
    image: *superset
    imagePullSecrets:
      - name: "registry-secret-name"    

  nlweb:
    image: *nlweb
    imagePullSecrets:
      - name: "registry-secret-name"

  nlwebflink:
    image: *nlwebflink
    imagePullSecrets:
      - name: "registry-secret-name"
  yugabyte:
    Image: *yugabyte
    imagePullSecrets:
      - name: "registry-secret-name"
  yugabytecql:
    image: *yugabytecql
    imagePullSecrets:
      - name: "registry-secret-name"

  janusgraph:
    image: *janusgraph
    imagePullSecrets:
      - name: "registry-secret-name"

  sync-tool:
    image: *sync_tool
    imagePullSecrets:
      - name: "registry-secret-name"

  secor:
    image: *secor
    imagePullSecrets:
      - name: "registry-secret-name"

  yugabyte-backup:
    image: *yugabyte_backup
    imagePullSecrets:
      - name: "registry-secret-name"

# Merge internal into root for direct access
<<: *internal

# grafana's real image path — the root-level `grafana:` key above never reaches it
kube-prometheus-stack:
  grafana:
    image: *grafana
    sidecar:
      image: *k8s_sidecar
  prometheusOperator:
    image: *prometheus_operator
    admissionWebhooks:
      patch:
        image: *kube_webhook_certgen
  prometheus:
    prometheusSpec:
      image: *prometheus
  alertmanager:
    alertmanagerSpec:
      image: *alertmanager
  prometheus-blackbox-exporter:
    image: *blackbox_exporter
  kube-state-metrics:
    image: *kube_state_metrics
  prometheus-node-exporter:
    image: *node_exporter
  statsd-exporter:
    image: *statsd_exporter

kubernetes-dashboard:
  api:
    image: *dashboard_api
  web:
    image: *dashboard_web
  metricsScraper:
    image: *dashboard_metrics_scraper

loki:
  image: *loki
  gateway:
    image: *loki_gateway

reloader:
  reloader:
    deployment:
      image: *reloader

# CAUTION: velero/janusgraph/knowledgemw are ALSO keys inside internal:
# (merged to root via <<: *internal above). YAML merge-key semantics mean an
# explicit key at the same name completely REPLACES the merged-in one — it
# does NOT deep-merge — so every block below must re-include that internal:
# entry's image/imagePullSecrets alongside whatever new field it's adding, or
# it silently deletes the working override that internal: already provided.
# (Found by testing: after adding these blocks, all three's *main* image
# silently reverted to the vendored chart's own default — velero's shows the
# same as intended only because upstream's own default happens to match.)

# velero.initContainers is a list, which Helm replaces wholesale rather than
# merging by key — replicated in full (unchanged apart from the image alias)
# rather than partially, to avoid silently dropping the other required fields
velero:
  image: *velero
  imagePullSecrets:
    - name: "registry-secret-name"
  initContainers:
    - name: velero-plugin-for-azure
      image: *velero_plugin_azure
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - mountPath: /target
          name: plugins
  kubectl:
    image: *velero_kubectl

# janusgraph.sidecars is a list, same reasoning as velero.initContainers
# above — replicated in full rather than partially
janusgraph:
  image: *janusgraph
  imagePullSecrets:
    - name: "registry-secret-name"
  sidecars:
    - name: logstash
      image: *janusgraph_logstash
      imagePullPolicy: Always
      env:
        - name: PIPELINE_WORKERS
          value: "1"
        - name: PIPELINE_BATCH_SIZE
          value: "1"
        - name: LS_JAVA_OPTS
          value: "-Dnetworkaddress.cache.ttl=30 -Dnetworkaddress.cache.negative.ttl=10"
      livenessProbe:
        httpGet:
          path: /
          port: 9600
        initialDelaySeconds: 60
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 3
      volumeMounts:
        - name: janusgraph-configs
          mountPath: /usr/share/logstash/pipeline/logstash.conf
          subPath: logstash.conf
        - name: empty-dir
          mountPath: /opt/bitnami/janusgraph/logs
          subPath: app-logs-dir

# Flat keys read directly off each building block's own root values (these
# umbrella-level templates were reading a hardcoded literal until now, see
# the anchors' comment above)
job_cleaner_kubectl_image: *job_cleaner_kubectl_image
job_wait_image: *job_wait_image
yugabyte_migration_image: *yugabyte_migration_image
opensearch_migration_image: *opensearch_migration_image

# envoy/opa/proxy_init are read inside the knowledgemw SUBCHART's own
# template, so .Values there resolves against edbb.knowledgemw.*, not
# edbb's own root — has to be nested here, unlike the three flat keys above
knowledgemw:
  image: *knowledge_mw
  imagePullSecrets:
    - name: "registry-secret-name"
  envoy_image: *envoy_image
  opa_image: *opa_image
  proxy_init_image: *proxy_init_image

# Flink images
global:
  # learnbb/knowledgebb/obsrvbb's templates/provision/kafka.yaml Job reads
  # global.kafka.image directly (a frozen snapshot of each bb's own local
  # kafka: anchor) instead of kafka.image above, so it never picked up the
  # override either
  kafka:
    image: *kafka

  knowledgeflink:
    registry: "ghcr.io"
    repository: "sunbird-knowlg/knowledge-platform-jobs"
    tag: "spark-v1.1.0_dec74d7"

  learnflink:
    registry: "ghcr.io/sunbird-lern"
    repository: "data-pipeline"
    tag: "spark-v1.1.0_f36189f"

  # ai-pipeline (PyFlink 1.20) jobs, deployed via the consolidated
  # `py-flink` chart's py_flink_jobs map (helmcharts/knowledgebb/charts/
  # py-flink) — each job entry there points at one of these anchors via
  # its own `globalImageKey`, since (unlike knowledgeflink/learnflink
  # above, which are one shared JVM image per building block selected via
  # --job-classname) each PyFlink job ships its own dedicated image. See
  # ai-pipeline/docker/Dockerfile.enrichment-router and
  # Dockerfile.caption-generator.
  #
  # Built and pushed by ai-pipeline's .github/workflows/build.yaml on tag
  # push — tag format is <git-tag>_<short-sha> (see knowlg-service.yml for
  # the same convention).
  enrichment_router:
    registry: "ghcr.io"
    repository: "sunbird-knowlg/ai-pipeline/enrichment-router"
    tag: "spark-v1.1.0_93887dd"

  caption_generator:
    registry: "ghcr.io"
    repository: "sunbird-knowlg/ai-pipeline/caption-generator"
    tag: "spark-v1.1.0_93887dd"
