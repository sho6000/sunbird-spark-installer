---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ .Release.Namespace }}
  annotations:
    {{- .Values.provisioningAnnotations | toYaml | nindent 4 }}
spec:
  template:
    metadata:
      labels:
        kong-admin-client: "true"
    spec:
      initContainers:
      - name: wait-for-kong
        image: busybox
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          timeout 600 sh -c '
            until nc -z {{ .Values.kong_admin_url }} {{ .Values.kong_admin_port }}; do
              echo "waiting for kong admin...";
              sleep 10;
            done;
            until wget -qO- http://{{ .Values.kong_admin_url }}:{{ .Values.kong_admin_port }}/status >/dev/null 2>&1; do
              echo "waiting for kong /status ...";
              sleep 5;
            done'
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.global.images.kong_api_scripts.registry }}/{{ .Values.global.images.kong_api_scripts.repository }}:{{ .Values.global.images.kong_api_scripts.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
          - /bin/sh
          - -c
          args:
            - python /kong_yaml_to_json.py && python /kong_consumers.py /tmp/kong-consumers.json --kong-admin-api-url=http://{{ .Values.kong_admin_url }}:{{ .Values.kong_admin_port }} --managed-by={{ .Values.managed_by }}
          volumeMounts:
          {{- if .Values.configmap.enabled }}
          - name: config
            mountPath: {{ .Values.configmap.mountPath }}
          {{- end }}
      volumes:
      {{- if .Values.configmap.enabled }}
      - name: config
        secret:
          secretName: {{ include "common.names.fullname" . }}
      {{- end }}
      restartPolicy: Never
  backoffLimit: 30
