.*.sw?
.idea
terragrunt.iml
vendor
.opentofu
.vscode
*.tfstate
*.tfstate.backup
*.out
.terragrunt-cache
.bundle
.ruby-version
terragrunt
.DS_Store
*.terrahelpbkp

# Ignoring common charts (both the extracted dir and the versioned .tgz
# archive helm dependency update actually produces, e.g. common-0.1.0.tgz)
**/charts/common
**/charts/common-*.tgz
# Ignoring test directories
**/kitchen

# Ignore built JARs for JanusGraph
scripts/janusgraph/*.jar

# OpenTofu/Terraform local init artefacts (created by `tofu init`, e.g. when running the CI validate step locally)
**/.terraform/
.terraform.lock.hcl
